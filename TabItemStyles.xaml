<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CarLeasingViewer.ResourceDictionaries">

    <Thickness x:Key="TabItemHeaderPadding" Left="10" Top="5" Right="10" Bottom="5"/>

    <DataTemplate x:Key="PeriodTabItem">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="За период с" Margin="10 0 5 0" VerticalAlignment="Center" />
                <ComboBox />
            </Grid>
        </Grid>
    </DataTemplate>
</ResourceDictionary>